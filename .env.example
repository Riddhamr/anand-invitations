# Neon Postgres (via Vercel integration)
# Set automatically when you add a Neon database in the Vercel dashboard.
# For local dev: copy to .env.local and paste the value from Vercel → Storage → your DB → .env.local tab.

POSTGRES_URL=
